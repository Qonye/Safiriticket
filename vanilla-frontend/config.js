// Define the base URL for your API.
// Make sure this file is loaded in your index.html before other scripts that make API calls.
// Restore the original format for all modules to work
// This should be the ONLY global variable for backend API base URL used app-wide:
window.API_BASE_URL = 'https://safiriticket.up.railway.app';
